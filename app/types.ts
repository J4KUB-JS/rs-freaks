export interface EventType {
  id: string;
  date: string;
  name: string;
  description: string;
  isMain: boolean;
  files: any;
}

export interface PostType {
  id: string;
  name: string;
  description: string;
  files: any;
}
